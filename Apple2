#include <iostream>

using namespace std;

struct Apple {
    char name[25];
    double d;
    double mass;

    void print() {
        cout << name << "\n" << d << "\n" << mass << endl;
    }

};

int main() {
    Apple *ps = new pizza;
    cout << "Enter company name:";
    cin.getline(ps->name, 25);
    cout << "Enter Apple diameter:";
    cin >> ps->d;
    cout << "Enter Apple mass:";
    cin >> ps->mass;
    ps->print();
    return 0;
}
